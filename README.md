# DEV INFO

Firstly, install the Python 3.12 and Poetry for installing the requirements.

Next step:

```
poetry install
```

If you need to add new requirement:

```
poetry add <package_name>
```

Docker commands:

```
docker-compose up --build
```

To shutdown container:

```
docker-compose down
```